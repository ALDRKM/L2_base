function createPhoneNumber(numbers){
  let str="";
  for (let i=0;i<numbers.length;i++){
    str+=numbers[i];
  }
  let num="("+str.slice(0,3)+") "+str.slice(3,6)+"-"+str.slice(6,10);
  return num;
  
}
console.log(createPhoneNumber([1, 2, 3, 4, 5, 6, 7, 8, 9, 0]));